# Python Package Dependencies for ODSC
#
# NOTE: The install.sh script uses SYSTEM PACKAGES (apt-get/dnf) instead of
# pip to install these dependencies for better system integration and to avoid
# externally-managed-environment issues on modern Linux distributions.
#
# This file is provided for:
# - Documentation of required packages
# - Alternative installation method: pip install -r requirements.txt
# - Development in virtual environments
# - CI/CD pipelines
#
# Dependencies are also listed in setup.py install_requires section.

# OneDrive API (using Microsoft Graph directly)
requests>=2.31.0

# File system monitoring
watchdog>=3.0.0

# GNOME/GTK GUI
PyGObject>=3.42.0

# D-Bus for inter-process communication
dbus-python>=1.3.2

# Configuration and utilities
python-dateutil>=2.8.2

# Desktop trash integration
send2trash>=1.8.0

# Security - Token encryption
cryptography>=41.0.0
keyring>=24.0.0
certifi>=2023.7.22

# Retry logic with exponential backoff and jitter
tenacity>=8.2.0
